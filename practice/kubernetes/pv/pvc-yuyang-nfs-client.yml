apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-yuyang-nfs-client
  namespace: yuyang
  labels:
    app: pvc-yuyang-nfs-client
spec:
  storageClassName: nfs-client
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 64Mi